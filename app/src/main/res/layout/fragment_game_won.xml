<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context="com.fovsol.tictactoe.GameWonFragment">

    <data>

    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <ImageView
            android:id="@+id/playerText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="24dp"
            android:layout_marginTop="32dp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:srcCompat="@drawable/player" />

        <ImageView
            android:id="@+id/playerImage"
            android:layout_width="90dp"
            android:layout_height="132dp"
            android:layout_marginEnd="24dp"
            android:baselineAlignBottom="false"
            android:paddingRight="15dp"
            android:paddingBottom="30dp"
            app:layout_constraintBottom_toBottomOf="@+id/playerText"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintHorizontal_bias="0.555"
            app:layout_constraintStart_toEndOf="@+id/playerText"
            app:layout_constraintTop_toTopOf="@+id/playerText"
            tools:srcCompat="@drawable/x" />

        <TextView
            android:id="@+id/gameDrawText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="32dp"
            android:gravity="center_horizontal"
            android:text="@string/won_string"
            android:textSize="32sp"
            app:layout_constraintEnd_toEndOf="@+id/playerImage"
            app:layout_constraintStart_toStartOf="@+id/playerText"
            app:layout_constraintTop_toBottomOf="@+id/playerText" />

        <ImageView
            android:id="@+id/playButton"
            android:layout_width="204dp"
            android:layout_height="92dp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/gameDrawText"
            app:srcCompat="@drawable/play" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>